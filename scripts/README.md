# TRACE Automation & Utility Scripts

- `sync-remotes.sh`: Pushes current HEAD to both `team` and `personal` remotes safely.
- `validate.js`: Runs full environment & dependency verification checks across the workspace.
